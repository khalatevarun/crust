# Skills

Vendored from two external skill collections for the codebase-modularity refactor:

- `codebase-design`, `improve-codebase-architecture`, `domain-modeling` — from [mattpocock/skills](https://github.com/mattpocock/skills)
- `architect`, `how`, `why`, `typescript-best-practices`, `principle-boundary-discipline`, `principle-model-the-domain` — from [cursor/plugins](https://github.com/cursor/plugins) (`pstack` skillset)

`architect` depends on `how` and `why`; `improve-codebase-architecture` depends on `codebase-design`. Both pairs are kept together for that reason.
